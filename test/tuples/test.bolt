@entryPoint
public func main() {
	let errorCode = getErrorCode();

	print(errorCode.item1)
	print(" ")
	print(errorCode.item2)
	printLine()
}

func getErrorCode(): (Int, StringSlice) {
	(404, "Not Found")
}