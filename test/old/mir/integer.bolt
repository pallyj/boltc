import intrinsics

@defaultIntegerRepr
public struct Int {
	var repr: i64

	public operator func add(b: Self): Self {
		Self(repr: 0)
	}
}

@entryPoint
public func main(i: Int) {
	let n: Int = 100;
}

func returnFive(): i64 { 5 }

func letDecl(): i64 {
	let meaningOfLife: i64 = 42
	meaningOfLife
}

func mul(byTwo n: i64): i64 {
	let multiplied = integer64Mul(n, 2)

	multiplied
}

func ma(n: i64): i64 {
	integer64Add(mul(byTwo: n), 1)
}

func test_tuples() {
	let tuple = (left: 404, right: 600)

	let tuple: (i64, i64) = (404, 606)
}

func varying() {
	let var n = 0;

	n = 5

	let var m: Int

	m = 10
	m += 5;
}