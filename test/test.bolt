import intrinsics

struct Int {
	internal var repr: i64
}

func main() {
	let i: Int = 0xffff_fffe;

	printInt32(i.repr);
	
	let b: i1 = true;

	let x: i32 = if b {
		let a: i64 = 1;

		a
	} else {
		let b: i16 = 0;

		b
	}
}