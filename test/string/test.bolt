@entryPoint
func main() {
	let hello_world = "Hello, World!"

	// Somethings up with the polymorphization...
	let hello = hello_world[0..<5]

	print(withLength: hello)
}

func print(withLength slice: StringSlice) {
	print(slice)
	print(" with length: ")
	printLine(slice.length())
}