let base = 0
let iterations = 10

@entryPoint
func main() {
	for (0..<iterations) {
        print(factorial(it % 20))
	}
}

func factorial(n: Int): Int {
	if n < 2 { 1 }
	else { n * factorial(n - 1) }
}