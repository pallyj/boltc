[package]
name = "boltcc"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
prelude = { path = "lib/prelude" }
header = { path = "lib/header" }
parser = { path = "lib/c/parser" }
syntax = { path = "lib/c/syntax" }
colored = "2.0.0"
clap = { version = "3.0.10", features = [ "derive" ] }
